export enum tagTypes {
  auth = "auth",
  user = "user",
  product = "product",
  order = "order",
  feedback = "feedback",
  comment = "comment",
}

export const tagTypeLists = [
  tagTypes.auth,
  tagTypes.user,
  tagTypes.product,
  tagTypes.order,
  tagTypes.feedback,
  tagTypes.comment,
];
